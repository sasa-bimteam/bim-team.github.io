import React from 'react'

import logo from '../assets/images/logo.png';

const Header = (props) => (
    <header id="header" className="alt">
        <span className="logo1"><img src={logo} alt="" /></span>
    </header>
)

export default Header
